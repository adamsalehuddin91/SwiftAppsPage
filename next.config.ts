import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    /* config options here */
    output: 'standalone',
    // Optional: Add strict mode or optimization settings if needed
    reactStrictMode: true,
};

export default nextConfig;
